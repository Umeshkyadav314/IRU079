import React from "react";
import App from "../src/App";

export default function IndexPage() {
  // Render the in-repo SPA entry. Later we can convert internal pages
  // to proper Next.js pages (e.g., /docs, /dashboard, etc.).
  return <App />;
}
