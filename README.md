
  # High-Fidelity Figma Prototype

  This is a code bundle for High-Fidelity Figma Prototype. The original project is available at https://www.figma.com/design/gKpqxS6lG9ruu75ubAnvmb/High-Fidelity-Figma-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  