/**
 * Minimal Next.js config to get started alongside existing Vite setup.
 * Keep this file small — further customization (image domains, rewrites)
 * can be added later.
 */
export default {
  reactStrictMode: true,
  swcMinify: true,
};
